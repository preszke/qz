<%- model_class = Lesson -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => Lesson %></h1>
</div>
<% if @lesson and @lesson.errors %>

<ul>

    <% @lesson.errors.full_messages.each do |err| %>

     <li><%= err %></li>

<% end %>

</ul>

<% end %>
<%= render :partial => 'form' %>

